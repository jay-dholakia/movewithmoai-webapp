import RequestedFeatures from "@/components/admin/requested-features/RequestedFeatures";

const page = () => {
  return (
    <div>
      <RequestedFeatures />
    </div>
  );
};

export default page;
